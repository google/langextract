fix: Remove extra blank line before OpenAILanguageModel class

This commit removes an extra blank line before the `OpenAILanguageModel` class definition in `langextract/inference.py`. This resolves a formatting issue reported by `pyink` and should allow the `format` job to pass.